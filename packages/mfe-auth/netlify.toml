[build]
  command = "npm run build"
  publish = "dist"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- . ../crm-types"

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
